{
  "$schema": "https://unpkg.com/open-mcp-guardrails@latest/dist/guardrails.schema.json",
  "rules": [
    { "type": "pii", "action": "block", "exclude": ["my_number", "ip_address"], "scope": ["filesystem__read_file"] },
    { "type": "secrets", "action": "block", "only": ["aws_access_key"] }
  ]
}
