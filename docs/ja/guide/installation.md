# インストール

## 要件

- [Node.js](https://nodejs.org/) >= 23.6.0（TypeScript 設定ファイルのネイティブ読み込みに必要）

## インストール

```bash
npm install open-mcp-guardrails
```

Bun の場合:

```bash
bun add open-mcp-guardrails
```

## 次のステップ

- [クイックスタート](/ja/guide/quick-start) — 最初の設定ファイルを作成して動作確認
